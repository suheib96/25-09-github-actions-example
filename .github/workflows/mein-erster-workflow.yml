name: mein erster workflow

on: workflow_dispatch

jobs:
    hello-world-job:
        runs-on: ubuntu-latest
        name: Hello world job
        steps:
            - name: Hello
              run: |
                echo "Hello World"
                date